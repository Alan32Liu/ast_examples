struct Fraction;           // Forward declaration
typedef struct Fraction FractionType;  // Typedef

int add(int, int);
int minus(int, int);
void set_fraction(FractionType *);
int validate_fraction(FractionType);
static int abs(int a);
